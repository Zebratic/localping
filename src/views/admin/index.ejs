<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LocalPing - Admin Panel</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {}
      }
    }
  </script>

  <style>
    :root {
      color-scheme: dark;
    }

    body {
      background: linear-gradient(135deg, #0f1419 0%, #1a1f2e 100%);
      min-height: 100vh;
    }

    .tab-button {
      transition: all 0.3s ease;
      position: relative;
      padding: 0.75rem 1.5rem;
      font-weight: 500;
    }

    .tab-button:not(.active) {
      color: #9ca3af;
    }

    .tab-button.active {
      color: #4f46e5;
    }

    .tab-button.active::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(135deg, #4f46e5 0%, #6366f1 100%);
    }

    .tab-content {
      display: none;
    }

    .tab-content.active {
      display: block;
      animation: fadeIn 0.3s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .monitor-card {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 0.75rem;
      padding: 1.5rem;
      transition: all 0.3s ease;
      cursor: pointer;
    }

    .monitor-card:hover {
      background: rgba(255, 255, 255, 0.08);
      border-color: rgba(79, 70, 229, 0.3);
    }

    .monitor-card.selected {
      background: rgba(79, 70, 229, 0.1);
      border-color: #4f46e5;
    }

    .status-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.375rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 500;
    }

    .status-badge.up {
      background-color: rgba(16, 185, 129, 0.1);
      color: #10b981;
    }

    .status-badge.down {
      background-color: rgba(239, 68, 68, 0.1);
      color: #ef4444;
    }

    .notification {
      position: fixed;
      top: 20px;
      right: 20px;
      min-width: 350px;
      border-radius: 0.5rem;
      padding: 1.5rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
      z-index: 1000;
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        transform: translateX(400px);
        opacity: 0;
      }
      to {
        transform: translateX(0);
        opacity: 1;
      }
    }

    .notification.success {
      background-color: rgba(16, 185, 129, 0.2);
      border: 1px solid #10b981;
      color: #d1fae5;
    }

    .notification.error {
      background-color: rgba(239, 68, 68, 0.2);
      border: 1px solid #ef4444;
      color: #fee2e2;
    }

    .chart-container {
      position: relative;
      height: 300px;
      margin-bottom: 1rem;
    }

    .form-input {
      background-color: rgba(0, 0, 0, 0.3);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 0.5rem;
      padding: 0.75rem;
      color: white;
      transition: all 0.3s ease;
    }

    .form-input:focus {
      outline: none;
      border-color: #4f46e5;
      background-color: rgba(79, 70, 229, 0.1);
      box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
    }

    .btn-primary {
      background: linear-gradient(135deg, #4f46e5 0%, #6366f1 100%);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      border: none;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
    }

    .btn-primary:disabled {
      opacity: 0.5;
      cursor: not-allowed;
      transform: none;
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.1);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.15);
    }

    .btn-danger {
      background: rgba(239, 68, 68, 0.2);
      color: #fca5a5;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      border: 1px solid #ef4444;
    }

    .btn-danger:hover {
      background: rgba(239, 68, 68, 0.3);
    }
  </style>
</head>
<body class="bg-gray-950 text-white">
  <!-- Header -->
  <header class="border-b border-gray-800 bg-black/40 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-2xl font-bold bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">
        LocalPing Admin
      </h1>
      <button onclick="logout()" class="btn-secondary">
        Logout
      </button>
    </div>
  </header>

  <!-- Navigation Tabs -->
  <div class="border-b border-gray-800 bg-black/20 sticky top-16 z-40">
    <div class="max-w-7xl mx-auto px-4 flex gap-8">
      <button class="tab-button active" onclick="switchTab('monitors')">
        📊 Monitors
      </button>
      <button class="tab-button" onclick="switchTab('incidents')">
        🚨 Incidents
      </button>
      <button class="tab-button" onclick="switchTab('settings')">
        ⚙️ Settings
      </button>
    </div>
  </div>

  <main class="max-w-7xl mx-auto px-4 py-8">
    <!-- Monitors Tab -->
    <div id="monitors" class="tab-content active">
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">
        <!-- Monitor List -->
        <div class="lg:col-span-1 min-h-[400px]">
          <div class="flex justify-between items-center mb-4">
            <h2 class="text-xl font-semibold">Monitors</h2>
            <button onclick="addNewMonitor()" class="btn-primary text-sm">
              + Add
            </button>
          </div>
          <div id="monitorList" class="space-y-2 max-h-[calc(100vh-300px)] overflow-y-auto">
            <!-- Monitor items will be populated here -->
          </div>
        </div>

        <!-- Monitor Details / Edit View -->
        <div class="lg:col-span-3">
          <div id="monitorDetails" class="hidden">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 h-full">
              <!-- Charts on the left -->
              <div>
                <h3 class="text-lg font-semibold mb-4" id="monitorName">Select a monitor</h3>
                <div class="chart-container">
                  <canvas id="statusChart"></canvas>
                </div>
                <div class="bg-gray-800/50 rounded-lg p-4">
                  <div class="grid grid-cols-2 gap-4">
                    <div>
                      <p class="text-gray-400 text-sm">Status</p>
                      <p class="text-xl font-bold"><span id="monitorStatus" class="text-green-400">Up</span></p>
                    </div>
                    <div>
                      <p class="text-gray-400 text-sm">Uptime</p>
                      <p class="text-xl font-bold"><span id="monitorUptime">99.9%</span></p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Edit form on the right -->
              <div>
                <h3 class="text-lg font-semibold mb-4">Edit Monitor</h3>
                <form id="editForm" class="space-y-4">
                  <div>
                    <label class="block text-sm font-medium mb-1">Monitor Name</label>
                    <input type="text" id="editName" class="form-input w-full" placeholder="e.g., Web Server">
                  </div>

                  <div>
                    <label class="block text-sm font-medium mb-1">Host</label>
                    <input type="text" id="editHost" class="form-input w-full" placeholder="192.168.1.1 or example.com">
                  </div>

                  <div>
                    <label class="block text-sm font-medium mb-1">Protocol</label>
                    <select id="editProtocol" class="form-input w-full">
                      <option value="ICMP">ICMP (Ping)</option>
                      <option value="TCP">TCP</option>
                      <option value="UDP">UDP</option>
                      <option value="HTTP">HTTP</option>
                      <option value="HTTPS">HTTPS</option>
                    </select>
                  </div>

                  <div class="grid grid-cols-2 gap-4">
                    <div>
                      <label class="block text-sm font-medium mb-1">Port</label>
                      <input type="number" id="editPort" class="form-input w-full" placeholder="Optional">
                    </div>
                    <div>
                      <label class="block text-sm font-medium mb-1">Interval (sec)</label>
                      <input type="number" id="editInterval" class="form-input w-full" min="5" max="3600">
                    </div>
                  </div>

                  <div>
                    <label class="block text-sm font-medium mb-1">Group</label>
                    <input type="text" id="editGroup" class="form-input w-full" placeholder="e.g., Network">
                  </div>

                  <div class="flex gap-2 pt-4">
                    <button type="button" onclick="saveMonitor()" class="btn-primary flex-1">
                      Save Changes
                    </button>
                    <button type="button" onclick="deleteMonitor()" class="btn-danger flex-1">
                      Delete
                    </button>
                    <button type="button" onclick="cancelEdit()" class="btn-secondary">
                      Cancel
                    </button>
                  </div>
                </form>
              </div>
            </div>
          </div>

          <!-- Empty state -->
          <div id="emptyState" class="text-center py-12">
            <p class="text-gray-400 text-lg">Select a monitor to view details and edit settings</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Incidents Tab -->
    <div id="incidents" class="tab-content">
      <div>
        <h2 class="text-2xl font-bold mb-6">Incidents</h2>
        <div id="incidentsList" class="space-y-4">
          <!-- Incidents will be populated here -->
        </div>
      </div>
    </div>

    <!-- Settings Tab -->
    <div id="settings" class="tab-content">
      <div>
        <h2 class="text-2xl font-bold mb-6">Settings</h2>
        <div class="bg-gray-800/50 rounded-lg p-6 max-w-2xl">
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-medium mb-2">Admin Password</label>
              <button onclick="changePassword()" class="btn-primary">
                Change Password
              </button>
            </div>

            <div class="border-t border-gray-700 pt-4">
              <label class="block text-sm font-medium mb-2">Monitoring Settings</label>
              <p class="text-gray-400 text-sm mb-4">Configure default monitoring behavior</p>
              <div class="space-y-3">
                <div>
                  <label class="block text-sm font-medium mb-1">Default Check Interval</label>
                  <input type="number" id="defaultInterval" class="form-input w-full" placeholder="60">
                  <p class="text-gray-500 text-xs mt-1">Seconds between checks</p>
                </div>
                <div>
                  <label class="block text-sm font-medium mb-1">Alert Cooldown</label>
                  <input type="number" id="alertCooldown" class="form-input w-full" placeholder="300">
                  <p class="text-gray-500 text-xs mt-1">Seconds to wait before alerting again</p>
                </div>
              </div>
            </div>

            <div class="border-t border-gray-700 pt-4">
              <h3 class="font-semibold mb-3">About</h3>
              <p class="text-gray-400 text-sm">LocalPing - Homelab Dashboard & System Monitor</p>
              <p class="text-gray-500 text-xs mt-1">Version 1.0.0</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Notifications -->
  <div id="notificationContainer"></div>

  <script src="/js/admin-new.js"></script>
</body>
</html>
